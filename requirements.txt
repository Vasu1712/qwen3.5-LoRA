# --- Hugging Face SPACE (inference + RAG) requirements ---
# torch is provided by the ZeroGPU runtime — do NOT pin it here.
# gradio is controlled by `sdk_version` in README.md — do NOT list it here.
# Training deps (trl, bitsandbytes, datasets) are NOT here on purpose — the Space
# only serves the model. Those live in requirements-train.txt for your GPU box.
#
# Qwen3.5 needs transformers v5 ("older versions will not work" — model card).
# 5.2.0 is the stable release with qwen3_5 support (same pin Unsloth uses).
# If the Space errors about missing linear-attention kernels at load time, add:
#   flash-linear-attention
#   causal_conv1d
spaces
transformers==5.2.0
peft>=0.11.0
accelerate>=0.30.0
pyyaml
qdrant-client
fastembed
