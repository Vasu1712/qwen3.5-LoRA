# --- LoRA TRAINING requirements for Qwen3.5-9B (GPU box / Colab / HF Jobs — NOT the Space) ---
# Install torch matching your CUDA first (e.g. from pytorch.org), then:
#   pip install -r requirements-train.txt
#
# Qwen3.5 needs transformers v5 ("older versions will not work" — model card).
# 5.2.0 is the stable release with qwen3_5 support (same pin Unsloth uses).
# NOTE: for FREE T4 training use colab_train_t4.ipynb (Unsloth) instead — this
# file serves the plain-PEFT bf16 path (Ampere+ GPUs: L4/A100).
transformers==5.2.0
trl>=0.12
peft>=0.11
accelerate>=0.30
datasets>=2.19
# bitsandbytes intentionally NOT included: QLoRA (4-bit) is not recommended for
# Qwen3.5 — train_qlora.py uses plain bf16 LoRA for this model instead.
