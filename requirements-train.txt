# --- QLoRA TRAINING requirements (for a GPU box / Colab — NOT the Space) ---
# Install torch matching your CUDA first (e.g. from pytorch.org), then:
#   pip install -r requirements-train.txt
transformers>=4.51
trl>=0.12
peft>=0.11
bitsandbytes>=0.43
accelerate>=0.30
datasets>=2.19
